---
description: "API reference for the GNativeVolumeMonitor element."
---

# GNativeVolumeMonitor

```tsx
import { GNativeVolumeMonitor } from "@gtkx/jsx/gio";
```

## Hierarchy

[GObject](.gtkx/reference/gobject/object.md) → [GVolumeMonitor](.gtkx/reference/gio/volume-monitor.md) → **GNativeVolumeMonitor**

## Props

`ref` receives the `Gio.NativeVolumeMonitor` instance. Every mutable property also has an `onNotify<Prop>` handler prop called with the new value when the property changes. Props inherited from ancestor elements are documented on their own pages.
