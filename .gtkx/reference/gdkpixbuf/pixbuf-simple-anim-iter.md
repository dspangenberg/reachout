---
description: "API reference for the GdkPixbufSimpleAnimIter element."
---

# GdkPixbufSimpleAnimIter

```tsx
import { GdkPixbufSimpleAnimIter } from "@gtkx/jsx/gdkpixbuf";
```

## Hierarchy

[GObject](.gtkx/reference/gobject/object.md) → [GdkPixbufAnimationIter](.gtkx/reference/gdkpixbuf/pixbuf-animation-iter.md) → **GdkPixbufSimpleAnimIter**

## Props

`ref` receives the `GdkPixbuf.PixbufSimpleAnimIter` instance. Every mutable property also has an `onNotify<Prop>` handler prop called with the new value when the property changes. Props inherited from ancestor elements are documented on their own pages.
