---
description: "Represents a value Animation can animate."
---

# AdwAnimationTarget

Represents a value `Animation` can animate.

```tsx
import { AdwAnimationTarget } from "@gtkx/jsx/adw";
```

## Hierarchy

[GObject](.gtkx/reference/gobject/object.md) → **AdwAnimationTarget**

## Props

`ref` receives the `Adw.AnimationTarget` instance. Every mutable property also has an `onNotify<Prop>` handler prop called with the new value when the property changes. Props inherited from ancestor elements are documented on their own pages.
