---
description: "GtkLayoutChild subclass for children in a GtkConstraintLayout."
---

# GtkConstraintLayoutChild

`GtkLayoutChild` subclass for children in a `GtkConstraintLayout`.

```tsx
import { GtkConstraintLayoutChild } from "@gtkx/jsx/gtk";
```

## Hierarchy

[GObject](.gtkx/reference/gobject/object.md) → [GtkLayoutChild](.gtkx/reference/gtk/layout-child.md) → **GtkConstraintLayoutChild**

## Props

`ref` receives the `Gtk.ConstraintLayoutChild` instance. Every mutable property also has an `onNotify<Prop>` handler prop called with the new value when the property changes. Props inherited from ancestor elements are documented on their own pages.
